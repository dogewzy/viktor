# LLM & Agent 框架
langchain>=0.3.0
langgraph>=0.2.0
langchain-openai>=0.2.0
langchain-deepseek>=1.0.0
langchain-core>=0.3.0

# Web 框架
fastapi>=0.115.0
uvicorn>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.20

# 钉钉 Stream SDK
dingtalk-stream>=0.18.0

# 数据库
sqlalchemy>=2.0.0
pymysql>=1.1.0

# SSH 隧道（内网数据库通过跳板机访问）
paramiko>=3.4.0

# K8s
kubernetes>=31.0.0

# 阿里云 SLS 日志查询
aliyun-log-python-sdk

# 外部证据连接器
redis>=5.0.0
oss2>=2.18.0
pika>=1.3.0
pymilvus>=2.4.0

# 网页对话附件读取与导出
pypdf>=5.0.0
python-docx>=1.1.0
openpyxl>=3.1.0

# 代码自省（.gitignore 解析）
pathspec>=0.12.0

# 跨项目 Debug 预置依赖
# Viktor 会浅 clone 其他项目做分析，通常不会安装目标项目自己的 venv。
# 这些依赖用于运行轻量复现脚本或 import 常见 parser/test 模块，不代表 Viktor 业务代码依赖它们。
# 注意：目标项目若依赖与 Viktor 冲突的版本（例如 parser 依赖 pydantic<2），不要在这里降级；
# 应优先写不依赖目标项目 DTO/服务栈的复现脚本，或使用隔离环境验证。
requests>=2.32.0
nameko==2.14.1
validators>=0.28.0

# 超长 Agent 回复 markdown -> HTML 渲染
markdown>=3.6

# Watchdog 定时调度
apscheduler>=3.10.0

# Temporal：issue-intake → coding-task 的 durable workflow 编排
temporalio>=1.28.0

# 登录鉴权（内网用户名密码 + 长效 JWT）
bcrypt>=4.1.0
PyJWT>=2.8.0

# 基础依赖
pydantic>=2.0.0
loguru>=0.7.0
pyyaml>=6.0.0
httpx>=0.28.0

# 通用测试 / lint 工具（C 层）
# coding task 测试阶段对 Python 项目的默认命令是 pytest / ruff，装进 Viktor 镜像；
# 预热 venv 开 --system-site-packages 后可直接复用，无需在执行循环里现装。
# 项目专属依赖仍由各仓库 venv 安装；JS 的 jest/vitest 不预装，按需在执行阶段装。
pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.6.0
